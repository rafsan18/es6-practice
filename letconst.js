// constant is declared if the value of the variable is fixed.

const numbers = [12, 34];
numbers[1] = 67;
numbers.push(45);
console.log(numbers);

// let is a scope variable.
// when var is used in scope it can be leak so let is used in the for loop ( let i = 0;)
